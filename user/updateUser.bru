meta {
  name: updateUser
  type: http
  seq: 5
}

patch {
  url: {{local}}/api/user/3
  body: json
  auth: bearer
}

auth:bearer {
  token: {{token}}
}

body:json {
  {
    "name": "Admin",
    "lastname": "Istrador"
  }
}

settings {
  encodeUrl: true
}
