meta {
  name: login
  type: http
  seq: 7
}

post {
  url: {{local}}/login
  body: json
  auth: inherit
}

body:json {
  {
      "username": "admin",
      "password": "admin123"
  }
}

settings {
  encodeUrl: true
}
