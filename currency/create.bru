meta {
  name: create
  type: http
  seq: 3
}

post {
  url: {{local}}/api/admin/currency
  body: json
  auth: bearer
}

auth:bearer {
  token: {{token}}
}

body:json {
  {
    "name":"Argentine Peso",
    "symbol": "$",
    "code": "ARS"
  }
}

settings {
  encodeUrl: true
}
